# 2026-09-13 데이터 무결성 개선 결과

## 작업 기준

- 역할: 데이터 무결성 executor
- 작업 경로: `/Users/yongjun/.codex/worktrees/2ea9/invest_bot`
- 시작 HEAD: `c96894e6929d303d90f3b06543db66913e37084a`
- 분석 기준: `9e54fe6f8a009cdb6adeb3b002d85422e33c1779`
- 로컬 브랜치: `codex/data-integrity-20260913`
- 원본 프로젝트 `/Users/yongjun/PycharmProjects/invest_bot`의 파일·설정·데이터는 변경하지 않았다.

## 구현 결과

### 관심종목 migration 등록

- 원본의 미등록 `20260703_000003_add_report_favorite_symbols.py`를 검토하고 동일한 내용으로 이 worktree에 포함했다.
- revision은 `20260703_000003`, down revision은 `20260612_000002`다.
- 새 SQLite DB의 head upgrade와 초기 schema만 있는 legacy SQLite DB의 stamp/upgrade를 각각 임시 DB로 검증했다.

### 투자자 수급 fact 보존

- `save_investor_daily`가 summary 첫 행만 저장하던 동작을 모든 유효 거래일 행 저장으로 변경했다.
- 거래일이 없거나 외국인·기관·개인 수급 값이 모두 없는 행은 저장하지 않는다.
- 빈 summary 응답은 요청일의 all-NULL fact나 placeholder symbol row를 생성하지 않는다.
- 동일 종목·거래일 재실행은 기존 repository upsert 계약을 사용하므로 중복 행을 만들지 않는다.
- 통합 리뷰 후 malformed 문자열 날짜와 존재하지 않는 날짜도 해당 행만 제외하도록 보강했다. 잘못된 한 행 때문에 같은 응답의 유효 거래일이 유실되지 않는다.
- 숫자 필드의 malformed 값과 `NaN`, `Infinity`, `-Infinity`는 NULL로 정규화한다. 같은 행에 하나 이상의 유한한 수급 값이 있으면 유효 값은 보존하며, 세 수급 값이 모두 무효이면 그 행을 제외한다.

### 종목 메타데이터 우선순위

- 일반 가격/수급 수집의 placeholder 값이 종목 마스터의 실제 종목명과 `KOSPI`/`KOSDAQ` 시장값을 덮어쓰지 않도록 repository upsert 규칙을 조정했다.
- 기존 미등록 symbol 계약은 유지한다. 일반 수집에서 처음 본 종목은 정규화된 종목코드를 이름으로, `domestic_stock`을 시장 placeholder로 등록한다.
- 이후 종목 마스터 동기화는 placeholder 이름/시장값을 실제 메타데이터로 승격할 수 있다.
- 실제 시장값끼리의 변경은 종목 마스터 같은 구체적 입력이 계속 반영될 수 있다.

### 부분 성공 보고

- `BatchCollectionResult`의 공개 필드와 status 값은 변경하지 않았다.
- 수집/저장 단계별 row count와 `saved_files`를 누적해, 후단 실패가 앞 단계의 실제 결과를 0/빈 목록으로 지우지 않도록 했다.
- 각 storage snapshot이 실제 저장된 직후 호출 단위 callback으로 경로를 기록한다. 전역/인스턴스 잔여 상태는 사용하지 않으며 기존 save 메서드의 반환값과 원래 예외 전파는 유지한다.
- fault injection으로 가격·종목정보 저장 후 수급 저장이 실패하는 경우를 만들었다. 첫 실행은 `failed`이면서 앞서 저장된 3개 파일과 실제 row count를 보고하고, 재실행은 5개 산출물을 완성했다.
- 같은 save 단계 안에서 첫 snapshot 후 두 번째 snapshot이 실패하면 첫 파일 1개를, 두 snapshot 후 DB dual-write가 실패하면 두 파일을 `saved_files`에 보존하는 fault injection도 추가했다.
- 동일 임시 DB에 재실행한 뒤 가격 60행과 수급 1행만 존재해 중복·누락이 없음을 확인했다.

## 변경 파일

- `migrations/versions/20260703_000003_add_report_favorite_symbols.py`
- `src/invest_bot/db/repositories.py`
- `src/invest_bot/db/write_path.py`
- `src/invest_bot/market/collector.py`
- `tests/test_data_migrations.py`
- `tests/test_db_write_path.py`
- `tests/test_domestic_stock_collector.py`
- `docs/operations/session_reports/2026-09-13_data.md`

## 검증

Python 3.13.13과 원본 프로젝트의 기존 `.venv`를 실행용으로만 사용했다. 패키지는 변경하지 않았다.

1. 핵심 회귀 및 migration

   ```bash
   PYTHONPATH=src /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python -m pytest tests/test_db_write_path.py tests/test_domestic_stock_collector.py tests/test_stock_master_sync.py tests/test_data_migrations.py -q
   ```

   결과: `25 passed`, Alembic 설정의 기존 `path_separator` deprecation warning 4건.

2. 연관 저장소·수집 회귀

   ```bash
   PYTHONPATH=src /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python -m pytest tests/test_db_frame_storage.py tests/test_repository_interfaces.py tests/test_symbol_lookup.py tests/test_kis_client.py tests/test_csv_storage.py tests/test_db_migration_scaffold.py tests/test_report_favorites.py -q
   ```

   결과: `31 passed`.

3. 문법 검증

   ```bash
   PYTHONPYCACHEPREFIX=/private/tmp/invest_bot_data_pycache /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python -m py_compile src/invest_bot/db/write_path.py src/invest_bot/db/repositories.py src/invest_bot/market/collector.py migrations/versions/20260703_000003_add_report_favorite_symbols.py tests/test_db_write_path.py tests/test_domestic_stock_collector.py tests/test_data_migrations.py
   ```

   결과: PASS.

4. 전체 테스트

   ```bash
   ln -s /Users/yongjun/PycharmProjects/invest_bot/.venv .venv
   PYTHONPATH=src .venv/bin/python -m pytest -q
   unlink .venv
   ```

   결과: `349 passed, 2 failed`, Alembic deprecation warning 4건. 임시 `.venv` symlink는 실행 후 제거했다.

5. 통합 리뷰 후 malformed 입력 및 동일 단계 부분 저장 회귀

   ```bash
   PYTHONPATH=src /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python -m pytest tests/test_db_write_path.py tests/test_domestic_stock_collector.py tests/test_stock_master_sync.py tests/test_data_migrations.py -q
   ```

   결과: `28 passed`, Alembic 설정의 기존 `path_separator` deprecation warning 4건. 유효 날짜 + malformed 날짜 + 불가능한 날짜, 유효 숫자 + malformed/non-finite 숫자가 섞인 응답, 두 번째 snapshot 실패, snapshot 후 DB dual-write 실패와 재실행을 포함한다.

## 남은 실패와 범위 구분

- 실패 2건은 `tests/test_init_db_script.py`의 기존 외부 종목 마스터 다운로드 의존성이다.
  - `test_init_db_script_runs_migrations_for_sqlite`
  - `test_init_db_script_upgrades_legacy_bootstrap_sqlite_db`
- 두 테스트 모두 migration 적용 뒤 `scripts/init_db.py`가 외부 KOSPI/KOSDAQ 종목 마스터를 받는 단계에서 DNS/network 오류로 실패했다.
- 해당 파일과 `scripts/init_db.py`는 QA/운영 역할 소유이므로 수정, skip, xfail 처리하지 않았다.
- 이 작업이 추가한 임시 DB migration 테스트는 외부 네트워크 없이 새 DB와 legacy DB 모두 통과한다.
- Ruff는 기존 환경에 설치되어 있지 않아 실행하지 못했다. 새 dependency는 추가하지 않았다.

## 통합 계약과 주의점

- 운영/대시보드는 `BatchCollectionResult`의 스키마 변경을 받을 필요가 없다.
- 다만 `status == "failed"`인 결과에서도 `*_rows`와 `saved_files`가 0/빈 목록이 아닐 수 있다. 이는 실패 전 완료된 단계의 실제 부분 성공을 뜻하므로 UI/로그에서 폐기하지 않아야 한다.
- collector의 save 메서드에는 batch 내부 추적용 optional private `_on_saved` 인자가 추가됐지만 기존 호출자는 변경할 필요가 없고 반환형/예외 계약도 동일하다.
- `status == "success"`이면서 `error`가 비어 있지 않은 기존 stock-info fallback warning 계약은 유지했다.
- 종목 메타데이터의 authoritative source는 종목 마스터다. `unknown`과 `domestic_stock`은 placeholder이며, 일반 수집은 이미 존재하는 KOSPI/KOSDAQ 값을 낮추지 않는다.
- 공통 README와 task 문서에는 위 부분 성공 의미와 종목 메타데이터 우선순위를 다음 통합 시 반영하는 것이 좋다.

## 후속 과제

- API 재시도/backoff 확대와 endpoint별 오류 분류.
- 정규화 DB의 단계별 저장 건수까지 별도로 노출하는 persistence result 확장. 현재 `*_rows`는 수집 응답 행 수이고, `saved_files`는 같은 단계 내부를 포함해 저장 성공이 확인된 snapshot 목록이다.
- PostgreSQL 실제 migration 검증과 운영 DB 적용은 이번 범위 밖이며 수행하지 않았다.
