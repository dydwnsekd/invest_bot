# QA 독립 검증 결과 (2026-09-13)

## 범위와 기준점

- 역할: 독립 `test-engineer` / `verifier`
- 작업 경로: `/Users/yongjun/.codex/worktrees/cdc4/invest_bot`
- 시작 HEAD: `c96894e6929d303d90f3b06543db66913e37084a` (detached, 변경 없음)
- 분석 기준: `9e54fe6f8a009cdb6adeb3b002d85422e33c1779`
- 작업 브랜치: `codex/qa-isolated-tests-ci`
- 제품 소스, 기능 작업 소유 테스트, 원본 프로젝트의 설정·데이터는 수정하지 않았다.

## 구현 결과

### 기본 테스트 격리

- `tests/conftest.py`가 DB URL, DB 자격 정보, KIS/Discord 자격 정보와 앱 역할 관련 런타임 환경 변수를 각 테스트 전에 제거한다.
- `external_network` 또는 `postgresql` opt-in marker가 없는 테스트에서 실제 socket 연결을 즉시 실패시킨다. 기본 suite가 우연히 운영 DB나 외부 API를 호출할 수 없다.
- `synthetic_stock_master` fixture는 실제 다운로드/cache 대신 두 종목의 결정적 CSV를 제공한다.
- opt-in 테스트는 `pytest.ini`의 marker로 분리되며 기본 `pytest`에서는 deselect된다. `skip` 또는 `xfail`은 추가하지 않았다.

### `init_db.py` 회귀 테스트 격리

- 기존 실패 재현: 시작 상태에서 `tests/test_init_db_script.py`는 worktree의 존재하지 않는 `.venv/bin/python`을 고정해 `2 failed` (`FileNotFoundError`)였다.
- 수정 후 subprocess는 `sys.executable`을 사용한다.
- 테스트별 임시 `app.yaml`, SQLite DB, 종목 마스터 CSV, sync state를 사용한다.
- subprocess bootstrap이 `AppSettings.from_file()`과 종목 마스터 위치만 테스트 임시 파일로 연결하고 `urllib.request.urlopen`을 fail-fast로 차단한다.
- 저장소의 `config/app.yaml`, 기본 종목 마스터 cache, 기본 sync state를 읽거나 덮어쓰지 않는다.
- 마이그레이션 revision 기대값은 현재 Alembic script head에서 읽는다. 데이터 역할이 `20260703_000003`을 통합하면 같은 테스트가 해당 head를 검증한다.

### CI와 실행 경로

- `scripts/run_tests.py --suite default`: 외부 네트워크와 PostgreSQL marker를 제외한 기본 suite.
- `scripts/run_tests.py --suite external-network`: 명시적인 실제 네트워크 검증 전용. 현재 이 marker를 가진 테스트는 없다.
- `scripts/run_tests.py --suite postgresql`: 격리 PostgreSQL migration 검증 전용.
- GitHub Actions는 Python 3.13과 기존 `requirements.txt`, `requirements-dev.txt`만 설치한다.
- 기본 pytest job과 `postgres:17` service를 쓰는 migration job을 분리했다.
- PostgreSQL 테스트는 `QA_POSTGRESQL_URL`이 PostgreSQL이며 host가 `127.0.0.1`, `localhost`, `postgres` 중 하나이고 DB 이름이 `_test`로 끝날 때만 migration을 실행한다.

## 검증 증거

### 실행 완료

1. 기준점 실패 재현

   ```bash
   /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python -m pytest tests/test_init_db_script.py -q
   ```

   결과: 수정 전 `2 failed`; 두 실패 모두 worktree `.venv/bin/python` 고정으로 인한 `FileNotFoundError`.

2. 초기화 스크립트 회귀 테스트

   ```bash
   /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python -m pytest tests/test_init_db_script.py -q
   ```

   결과: `2 passed`, Alembic 설정 deprecation warning 1건.

3. 격리 관련 표적 suite

   ```bash
   /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python scripts/run_tests.py --suite default tests/test_settings.py tests/test_stock_master_sync.py tests/test_init_db_script.py -q
   ```

   결과: `14 passed`, warning 1건.

4. 기본 전체 suite

   ```bash
   /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python scripts/run_tests.py --suite default -q
   ```

   결과: `342 passed, 1 deselected`, warning 1건. deselected 1건은 별도 PostgreSQL migration 테스트다.

5. PostgreSQL migration suite

   ```bash
   QA_POSTGRESQL_URL=postgresql+psycopg://invest_bot_test:invest_bot_test@127.0.0.1:55439/invest_bot_test \
     /Users/yongjun/PycharmProjects/invest_bot/.venv/bin/python scripts/run_tests.py \
     --suite postgresql tests/integration/test_postgresql_migrations.py -q
   ```

   결과: 임시 `postgres:17` 컨테이너에서 `1 passed`, Alembic 설정 deprecation warning 2건. 최초 sandbox 실행은 localhost socket 권한으로 실패했으며, 승인된 동일 로컬 명령의 재실행 결과가 최종 증거다. 마지막 재검증 컨테이너 `invest-bot-qa-cdc4`는 `--rm`으로 시작했지만 stop 권한 재승인이 거부되어 실행 중일 수 있다. 종료 권한을 확보한 환경에서 `docker stop invest-bot-qa-cdc4`로 중지하면 자동 제거된다.

### 정적 확인

- CI YAML은 `yaml.safe_load`로 파싱한다.
- 변경 Python 파일은 `python -m py_compile`로 검사한다.
- 새 dependency는 추가하지 않았다.

## 알려진 기준점 결함 재현과 통합 후 계약

아래는 기능 역할이 소유한 결함이므로 QA 브랜치의 기본 suite에 실패 테스트를 추가하거나 `skip`/`xfail`로 숨기지 않았다.

### 1. 리포트 기준일 혼합

- 기준점 재현: 신호 `2026-04-10`, 투자자 수급 `2026-04-11`, 지표 `2026-04-12`를 전달하면 리포트 `date`는 `2026-04-10`이지만 `close=120`과 `foreign_net=1`은 미래 행에서 선택됐다.
- 통합 후 기대: 선택 기준일보다 늦은 지표·신호·수급 행은 제외한다. 역순 입력도 날짜 정렬 후 동일 결과여야 하며, 결측 또는 공통 기준일 부재는 명시적인 상태/오류가 되어야 한다.
- 통합 후 실행 후보:

  ```bash
  python -m pytest tests/test_market_report_generator.py tests/test_run_market_report.py -q
  ```

### 2. 보유 중 낙폭

- 기준점 재현: 한 거래가 `100 → 50 → 100`으로 끝나면 일별 평가금액은 `[100, 50, 100]`이나 거래 종가 수익률 기반 summary MDD는 `0.0%`였다.
- 통합 후 기대: summary와 일별 곡선 모두 MDD `50.0%`. 첫 거래가 초기자산보다 하락한 경우도 초기자산 peak 기준으로 포함한다.
- 통합 후 실행 후보:

  ```bash
  python -m pytest tests/test_backtest_generator.py tests/test_backtest_persistence.py tests/test_backtest_portfolio.py -q
  ```

### 3. 수급 여러 날짜

- 기준점 재현: `2026-01-03`, `2026-01-02` 두 summary 행을 저장하면 DB에는 첫 행 `2026-01-03` 한 건만 남았다.
- 통합 후 기대: 유효한 모든 날짜가 보존되고 `(symbol, trade_date)` 기준 재실행은 중복 없이 갱신된다. 빈 응답은 임의의 `target_date` fact를 만들지 않는다.
- 통합 후 실행 후보:

  ```bash
  python -m pytest tests/test_db_write_path.py tests/test_domestic_stock_collector.py -q
  ```

### 4. 부분 저장 후 실패·재실행

- 기준점 재현: daily/stock CSV 저장 후 investor 저장이 실패하면 실제 CSV 3개가 존재하지만 결과는 `status="failed"`, `saved_files=[]`였다.
- 통합 후 기대: 실패 결과가 이미 저장된 artifact/DB 단계를 정확히 보고한다. 동일 입력 재실행은 고유키 기준 중복을 만들지 않고 미완료 단계부터 안전하게 수렴한다.
- 통합 후 실행 후보:

  ```bash
  python -m pytest tests/test_domestic_stock_collector.py tests/test_db_write_path.py tests/test_scheduled_collection.py -q
  ```

## 다른 역할이 받아야 할 계약

- 운영 역할: 완료 커밋 `7e6c9d300b8906fefe476a288e8585db6af3befc`의 계약은 `scripts/init_db.py --mode migrate-only|full`, `INVEST_BOT_INIT_MODE=migrate-only|full`, 우선순위 `CLI > 환경변수 > 기본 full`이다. 통합 후 QA는 임시 `INVEST_BOT_DATABASE_URL`과 `sys.executable`로 `--mode migrate-only`를 실행해 종목 마스터 refresh, sync state 기록, 외부 네트워크가 없음을 검증해야 한다. 별도 테스트로 환경변수보다 CLI가 우선하고, CLI 부재 시 환경변수, 둘 다 부재 시 기존 full 동작이 선택되는지 확인한다. QA 브랜치에는 운영 커밋을 cherry-pick하지 않았다.
- 데이터 역할: `20260703_000003` migration을 포함하고 PostgreSQL job에서 Alembic head와 `report_favorite_symbols` 생성을 검증해야 한다. 여러 날짜 수급 저장 및 부분 성공 결과 계약을 기능 테스트로 소유한다.
- 퀀트 역할: 리포트 기준일과 일별 mark-to-market MDD를 기능 테스트로 소유한다. summary MDD와 persisted daily equity의 값이 일치해야 한다.
- 통합 작업: 기능 브랜치 통합 후 위 네 묶음 명령, 기본 suite, PostgreSQL suite 순서로 재실행한다. `README.md`, `DESIGN.md`, 공통 task 문서 갱신 제안은 이 보고서의 실행 경계와 명령을 반영한다.

## 남은 주의점

- 현재 QA 브랜치에는 데이터 역할 소유의 미등록 `20260703_000003` migration이 없으므로 이 테이블 생성은 QA 단독 결과로 주장하지 않는다.
- Alembic `prepend_sys_path`의 `path_separator` deprecation warning은 기존 `alembic.ini`에서 발생하며 이번 파일 소유 범위 밖이다.
- 실제 외부 API, Discord, 주문, 운영 DB, 원격 push/배포는 실행하지 않았다.
