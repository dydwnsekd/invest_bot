[pytest]
testpaths = tests
pythonpath = src
cache_dir = .tmp/pytest_cache
addopts = -m "not external_network and not postgresql"
markers =
    external_network: opt-in test that may access a real external network
    postgresql: opt-in integration test that requires an isolated PostgreSQL database
