symbols:
  - "005930"
  - "000660"
days: 365
interval_minutes: 1440
run_on_startup: true
# Relative paths are resolved from this config file. This keeps the log outside
# the read-only config mount and inside the shared Compose runtime volume.
log_path: ../.runtime/logs/collection_scheduler.log
